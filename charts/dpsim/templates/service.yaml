apiVersion: v1
kind: Service
metadata:
  name: {{ include "dpsim.fullname" . }}
  labels:
    {{- include "dpsim.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dpsim.selectorLabels" . | nindent 4 }}
